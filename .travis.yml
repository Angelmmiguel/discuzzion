language: node_js
node_js:
  - "7"
cache: yarn
install:
  - yarn install
  - cd client && yarn install && cd ../
script:
  - cd client && yarn test
  - cd client && yarn run build
  - cd client && yarn run test --coverage && cat ./coverage/lcov.info | ./node_modules/.bin/codacy-coverage -p .
